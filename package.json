{
  "name": "sanity-certification",
  "private": true,
  "version": "1.0.0",
  "description": "A project for sanity certification",
  "author": "Muhammad Bashir",
  "license": "ISC",
  "scripts": {
    "dev": "pnpm run --parallel dev",
    "build": "pnpm run --parallel build",
    "test": "pnpm run --parallel test"
  },
  "engines": {
    "node": ">=20.0.0",
    "pnpm": "10"
  }
}